npm install -g for nodemon globally
to see where those files are - find the root of npm global
npm root -g
C:\Users\PC\AppData\Roaming\npm\node_modules

CTRL+` to open terminal
CTRL+L to clear terminal
SHFT+ALT+F to format files

Emmet - autocomplete    type h1 + tab  in .handlebars files

had an issue connecting to db.  went into bin directory where mongo exe was and ran this:
  mongod --dbpath C:\data\db
  NOTE: MUST OPEN TERINAL AS ADMIN AND NAVIGATE TO MONGO/BIN THEN RUN mongod
    probably need to start mongo first